# Bayelle-static-Website
Static website to signify that the website is under construction
install dependacies "npm install -D vite @vitejs/plugin-react"
run the app in there terminal with the code "npm run dev"